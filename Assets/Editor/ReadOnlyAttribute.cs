using UnityEngine;

public class ReadOnlyAttribute : PropertyAttribute
{
    // This attribute itself doesn't need any special logic,
    // its existence is enough for the PropertyDrawer to recognize it.
}